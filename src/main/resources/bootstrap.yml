spring:
  application:
    name: araok
  cloud:
    config:
      uri: http://localhost:5000
      enabled: true
#spring:
#  devtools:
#    add-properties: false
#  config:
#    import: optional:configserver:http://localhost:5000