spring:
  data:
    rest:
      base-path: /rest
  application:
    name: media-ms
  datasource:
    url: jdbc:postgresql://localhost:5432/araok-media
    username: araok
    password: araok
    driver-class-name: org.postgresql.Driver

  flyway:
    baseline-on-migrate: true
    enabled: true
    url: jdbc:postgresql://localhost:5432/araok-media
    user: araok
    password: araok

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  health:
    defaults:
      enabled: true

server:
  port: 8092